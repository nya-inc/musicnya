/* eslint-disable @typescript-eslint/no-unsafe-call */
import { musickit } from './musickit';

describe('musickit', () => {
  it('should work', () => {
    expect(musickit()).toEqual('musickit');
  });
});
